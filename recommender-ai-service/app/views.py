"""
AI Assistant Service – Legacy Django Views (NOT SERVED)
─────────────────────────────────────────────────────────
All HTTP endpoints are served by FastAPI (main.py → app/api/routes.py).
Django is used only for ORM/migrations. This file is intentionally empty.
"""
import logging
logger = logging.getLogger(__name__)
